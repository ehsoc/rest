package resource_test

import (
	"testing"
)

func TestGetQuery(t *testing.T) {
	// r, _ := http.NewRequest("POST", "/?foo=1&foo=2", nil)
	// p := resource.NewQueryArrayParameter("foo", nil)
	// parameters := make(map[resource.ParameterType]map[string]resource.Parameter)
	// parameters[p.HTTPType] = make(map[string]resource.Parameter)
	// parameters[p.HTTPType][p.Name] = p
	// input := resource.Input{parameters}
	// querySlice := input.GetQuery(r, p.Name)
	// if len(querySlice) != 2 {
	// 	t.Fatalf("got :%v want: %v", len(querySlice), 2)
	// }

}
